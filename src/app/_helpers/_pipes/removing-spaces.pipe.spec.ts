import { RemovingSpacesPipe } from './removing-spaces.pipe';

describe('RemovingSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovingSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
