import { FullstateToAbvPipe } from './fullstate-to-abv.pipe';

describe('FullstateToAbvPipe', () => {
  xit('create an instance', () => {
    // const pipe = new FullstateToAbvPipe();
    // expect(pipe).toBeTruthy();
  });
});
