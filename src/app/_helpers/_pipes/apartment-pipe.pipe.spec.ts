import { ApartmentPipePipe } from './apartment-pipe.pipe';

describe('ApartmentPipePipe', () => {
  xit('create an instance', () => {
    const pipe = new ApartmentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
