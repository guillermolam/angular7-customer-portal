import { ViewableExtensionPipe } from './viewable-extension.pipe';

describe('ViewableExtensionPipe', () => {
  xit('create an instance', () => {
    const pipe = new ViewableExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
